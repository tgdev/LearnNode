mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    
    h2 I forgot my password
    
    label(for="email") Email address
    input(type="email" name="email")
    
    input.button(type="Submit" value="Request reset")
